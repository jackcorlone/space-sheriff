# 参与贡献

感谢你帮助改进空间卫士。

## 开发环境

- Go 1.24 或更高版本，用于推荐的便携版
- Python 3.12 或更高版本，用于原生窗口版和测试
- macOS 原生窗口版需要新版 Tk

## 本地检查

```bash
cd portable
go test ./...
go vet ./...
```

```bash
python -m unittest discover -s tests -v
```

## 提交建议

1. 从 `main` 创建一个短期分支。
2. 只修改与问题直接相关的代码。
3. 为新规则、扫描行为或安全修复添加测试。
4. 确保 Go 与 Python 测试均通过。
5. 提交 Pull Request，并说明行为变化和验证方式。

涉及清理规则的修改必须保持保守：无法确定用途时，选择“需人工确认”，不要自动建议删除。
