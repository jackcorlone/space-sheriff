# Changelog

本项目的显著变更记录在此文件中。版本号遵循
[Semantic Versioning](https://semver.org/)。

## Unreleased

### Added

- 每日或每周定时只读扫描计划
- macOS LaunchAgent 与 Windows Task Scheduler 当前用户任务注册
- 不启动浏览器或 HTTP 服务的 `--scheduled-scan` 单次运行模式
- 计划扫描历史、策略快照和有限的大文件结果详情
- SQLite 跨进程计划租约与过期恢复
- 计划预计下次运行、失败/错过运行提醒和系统任务修复入口
- 便携版界面新增离线 SVG 图标、扫描概览卡片、空间画像和扫描质量面板
- 直接打开静态 `file://` 页面时显示本地后端连接说明并禁用扫描操作；新增 macOS/Windows 本地开发启动脚本
- v0.9 原型新增新手分析指南、带摘要的 JSON 导出、带可读大小/下一步动作的 CSV 导出和导出成功提示
- v1.0 稳定候选新增 CLI 帮助/版本输出、API/schema 版本信息、错误分类与有限诊断样本、JSON 诊断摘要和可访问性基线

### Changed

- SQLite schema 升级到 v3，扫描会话记录触发来源和策略版本
- 中断恢复只处理超过租约窗口的陈旧任务，避免误改其他活动进程
- 长时间扫描定期续订租约；编辑计划保留自定义阈值并自动刷新历史
- JSON API 接受标准 charset 参数，同时拒绝未知字段和尾随值
- 扫描根目录符号链接在开始前解析为真实目录
- 计划界面检查 macOS LaunchAgent 与 Windows Task Scheduler 的缺失或参数漂移
- 索引扫描按本次会话的文件尺寸逐组处理重复候选，先计算首尾快速指纹再计算完整 SHA-256
- 完整哈希和索引写入支持可取消的 SQLite 批次；取消时不会提交半批记录
- 交互式与定时扫描增加逻辑字节和最长时间预算，达到预算时保留结果并记录 `budget_exceeded`
- SQLite schema 升级到 v4，计划保存可选的扫描预算
- 新增文件量扫描基准入口，不在缺少实测数据时承诺固定性能
- 扫描质量面板区分读取错误与规则排除；预算为 0 明确标记为不限制
- 导出前要求完成或停止一次扫描，避免生成空报告；导出仍由浏览器在本机下载，不上传远程服务
- 冻结 v1.0 的 SQLite schema v4、CLI 参数和 v1 HTTP API 契约；诊断样本只保留在当前扫描状态与交互式 JSON 中
- UI 增加跳过链接、可见焦点、状态播报和 `prefers-reduced-motion` 支持

### Security

- 定时入口不调用清理、回收站或清理计划写入
- 系统计划任务只继承当前用户权限，不注册系统级服务
- 系统/应用目录在建议和实际清理入口均受保护，旧扫描结果中的系统安装资源也不能移入回收站

## 0.2.0 - 2026-07-28

### Added

- 目录空间聚合与逐层下钻，快速定位占用最大的文件夹
- 文件名称搜索、风险与类型筛选、大小与时间排序
- 最多 500 个扫描结果的批量回收站清理
- 更细分且带稳定规则编号的离线清理建议

### Security

- 每次启动生成本地 API 会话令牌
- 清理前重新核对文件类型、大小和修改时间，阻止处理扫描后已变化的文件

## 0.1.0 - 2026-07-27

### Added

- Windows x64、Windows ARM64、macOS Apple Silicon 和 macOS Intel 便携版
- 本地磁盘容量与可用空间检测
- 大文件扫描、大小排序、路径和最后修改时间展示
- 离线、可解释的删除风险判断
- 系统与应用目录保护
- Windows 和 macOS 系统回收站集成
- macOS Apple Silicon 原生窗口版
- 自动测试、跨平台构建与 GitHub Release 工作流
